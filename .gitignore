# Go build cache and scratch directories used by the build/self-test scripts.
.gocache/
.gotmp/

# Built binaries. `./scripts/build.sh` (or scripts\build.ps1) recreates them in
# dist/, and the deploy flow copies them to the server from there. Commit them
# with `git add -f dist` if you want release artifacts in the repository.
dist/

# Editor and OS noise.
.DS_Store
Thumbs.db
*.swp

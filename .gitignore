# Go build cache and scratch directories used by the build/self-test scripts.
.gocache/
.gotmp/

# Build leftovers (a binary that was still running when the build replaced it).
*~

# Editor and OS noise.
.DS_Store
Thumbs.db
*.swp

# Operator SSH credentials are local-only and must never enter the repository.
sshkeys/
